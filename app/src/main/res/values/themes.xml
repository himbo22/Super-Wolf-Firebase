<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.SuperWolfFirebase" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.SuperWolfFirebase" parent="Base.Theme.SuperWolfFirebase" />

    <style name="Theme.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenAnimatedIcon">@drawable/werewolfback</item>
        <item name="windowSplashScreenBackground">@color/soft_pink</item>
        <item name="postSplashScreenTheme">@style/Base.Theme.SuperWolfFirebase</item>
    </style>

    <style name="TextInputLayoutStyle" parent="Widget.MaterialComponents.TextInputLayout.OutlinedBox.Dense.ExposedDropdownMenu">
        <item name="boxStrokeColor">@color/dark_pink</item>
        <item name="boxStrokeWidth">2dp</item>
    </style>

</resources>